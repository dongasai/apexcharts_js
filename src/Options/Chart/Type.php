<?php

namespace DApexchartsJs\Options\Chart;

use MyCLabs\Enum\Enum;
use DApexchartsJs\helper\EnmuCalss;


/**
 * @method static Type line()
 * @method static Type area()
 * @method static Type bar()
 * @method static Type pie()
 * @method static Type donut()
 * @method static Type radialBar()
 * @method static Type scatter()
 * @method static Type bubble()
 * @method static Type heatmap()
 * @method static Type boxPlot()
 * @method static Type candlestick()
 * @method static Type radar()
 * @method static Type polarArea()
 * @method static Type rangeBar()
 * @method static Type rangeArea()
 * @method static Type treemap()
 */
class Type extends Enum
{

    private const radar = 'radar';
    private const polarArea = 'polarArea';
    private const rangeBar = 'rangeBar';
    private const rangeArea = 'rangeArea';
    private const treemap = 'treemap';

    private const line = 'line';
    private const area = 'area';
    private const bar = 'bar';
    private const pie = 'pie';
    private const donut = 'donut';
    private const radialBar = 'radialBar';
    private const scatter = 'scatter';

    private const bubble = 'bubble';

    private const heatmap = 'heatmap';

    private const candlestick = 'candlestick';

    private const boxPlot = 'boxPlot';



}
